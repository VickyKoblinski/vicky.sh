# vicky.sh

Personal website, built with React, TypeScript, Tailwind CSS, Vite, and Vitest.

## Development

```sh
npm install
npm run dev
```

## Checks

```sh
npm run lint
npm test
npm run build
```

GitHub Actions runs these checks for pull requests and pushes to `main`. It does not deploy the site.
